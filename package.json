{
  "name": "rag-pipeline",
  "private": true,
  "description": "Convenience wrapper for Python dependency setup in this repo",
  "scripts": {
    "postinstall": "node scripts/setup-python-env.js"
  }
}
